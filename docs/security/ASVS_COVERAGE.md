# ASVS 5.0 Coverage

Last reviewed: 2026-06-21.

Scope: Work Archive API, web runtime headers, production compose, and security
operations artifacts. This is a coverage map, not a certification claim.

OWASP ASVS 5.0 chapter references follow the published ASVS 5.0 taxonomy:
<https://cornucopia.owasp.org/taxonomy/asvs-5.0>.

Status values:

- `satisfied`: implemented and covered by code, tests, or release procedure.
- `partial`: implemented but missing matrix coverage, operational evidence, or
  a planned hardening step.
- `gap`: not implemented or intentionally deferred.

## Coverage Matrix

| Area                             | ASVS 5.0 reference                                                  | Status    | Current evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Remaining work                                                                                                                                                                                                                                             |
| -------------------------------- | ------------------------------------------------------------------- | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Authentication                   | V6 Authentication, V10 OAuth and OIDC                               | partial   | Google-only OAuth state and nonce cookies are generated and verified in `apps/api/src/modules/auth/auth.controller.ts`; Google ID tokens validate issuer, audience, algorithm, nonce, and email verification in `apps/api/src/modules/auth/auth.service.ts`; Work Archive access/refresh JWTs are issued and verified with fixed `HS256`, issuer `work-archive-api`, audience `work-archive-web`, required `jti`/`iat`/`exp` registered claims, TTL upper-bound and future-issued-token checks, safe `sub`/`sid`/`email` identity claim shape, current-user email claim matching, and token-kind claim shape checks so access tokens cannot carry refresh-only `rememberMe` and refresh tokens must carry it; authenticated rate-limit user-keying and import-search authenticated bucket selection reject bearer tokens outside that boundary; legacy email/password routes return `410 Gone`; `docs/security/OAUTH_POLICY.md` and `npm run qa:oauth-policy` block drift in Google-only login, disabled legacy password routes, OAuth return-origin allowlisting, one-time flow state validation, cookie scope, token/code log-safety tests, and Gate 1 evidence wiring.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Attach live OAuth login/logout evidence to each release artifact; consider MFA policy only if the product scope changes beyond Google identity.                                                                                      |
| Session management               | V7 Session Management, V9 Self Contained Tokens                     | partial   | Access tokens are short-lived JWTs; refresh tokens are stored in `HttpOnly`, `Secure`, `SameSite=Strict`, `Path=/api/auth` production cookies, hashed server-side, rotated, revocable per session, and reuse revokes all sessions in `auth.service.ts`; the Google OAuth flow cookie is short-lived, `HttpOnly`, `Secure`, `SameSite=Lax`, and scoped to `Path=/api/auth/google`; cookie security comes from `auth.cookies.ts`, `auth-google-oauth.ts`, runtime `COOKIE_SECURE`, and `npm run qa:auth-session-policy`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Add release evidence for session revocation and refresh reuse tests.                                                                                                                                                                                       |
| Access control                   | V8 Authorization                                                    | partial   | Work read/update/delete paths are current-user scoped through `WorksService`, `UserRecordsService.updateActiveForUser`, and `JwtAuthGuard`; release-record REST mutations first load records through an owned parent work record; import provider credentials, Notion connections, and catalog submissions are documented in `docs/security/BOLA_MATRIX.md`; sync push checks owner mismatch for work, release records, timeline entries, personal graph entities, and tier board entities; pull queries are user-scoped; `docs/security/API_AUTHORIZATION_SURFACE.md` classifies every controller as protected, optional bearer, public health, metrics bearer token, or policy-bounded public image proxy; `npm run qa:api-auth-surface` blocks unclassified controllers and high-risk route guard drift; `npm run qa:bola-matrix` blocks missing sync object rows, unresolved BOLA statuses, and standalone controllers for object families marked `not_exposed` outside sync.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Attach BOLA and API authorization surface test evidence to each release artifact and keep both matrices current when adding controllers or sync object families.                                                                                            |
| Input validation                 | V2 Validation and Business Logic, V4 API and Web Service            | satisfied | Nest `ValidationPipe` enables transform, whitelist, `forbidNonWhitelisted`, and validation error `target`/`value` suppression in `apps/api/src/configure-app.ts`; unknown DTO fields are rejected instead of silently stripped and invalid DTO values are not echoed in validation error bodies; overlong path/query request targets are rejected before controller handling with a sanitized `414` response and bounded security audit metadata; malformed JSON and oversized request bodies are rejected before DTO validation with sanitized JSON `400`/`413` responses; non-empty unsafe `/api/*` request bodies with unsupported media types are rejected with sanitized `415` responses and bounded audit metadata; sync payloads use DTO validation with whitelist and `forbidNonWhitelisted`; runtime config validates env shape and numeric/boolean values in `api-runtime-config.ts`; `docs/security/API_INPUT_CONTRACTS.md` defines the request target length, body parser failure, request body media type, controller DTO, query DTO, sync strict payload, and provider credential exception contract; `npm run qa:api-input-contracts` blocks loose `@Body()` contracts, missing body DTO Swagger metadata, untyped query DTOs, sync strict-validation drift, validation value echo drift, request target guard/body parser/media type drift, and provider credential exception drift.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Maintain DTO validation on every new mutation and sync entity; add property tests only when validation logic becomes complex.                                                                                                                              |
| Output encoding and sanitization | V1 Encoding and Sanitization, V3 Web Frontend Security              | partial   | React output is encoded by default; API stores normalized strings; nginx CSP restricts scripts to self and blocks objects/frames in `apps/web/nginx.conf`; API responses keep a Helmet-backed security header floor documented in `docs/security/API_SECURITY_HEADERS.md`, and `npm run qa:api-security-headers` blocks drift in `x-powered-by` removal, CSP, no-sniff, referrer-policy, HSTS, and focused e2e header coverage.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Remove `style-src 'unsafe-inline'` only after the CSP report-only plan proves compatibility; see `docs/security/CSP_HARDENING_PLAN.md`.                                                                                                                    |
| API hardening                    | V4 API and Web Service                                              | partial   | Global `/api` prefix; explicit JSON and URL-encoded request body limits; bounded Node HTTP request/header/keep-alive timeouts; credentialed CORS uses an explicit origin, method, request-header, exposed-header, and preflight max-age contract; production fetch metadata guard; production origin guard; client header guard `off/audit/enforce`; `Cache-Control: no-store` defaults for normal `/api/*` responses and operational health endpoints, with a documented cacheable `/api/image-proxy` exception and metrics `no-store`; global `/api`, route-family, unsafe mutation, and sensitive auth operation rate limits for auth, catalog, sync, imports, image proxy, work/user-record mutations, and Notion; request IDs and security audit events in `security-middleware.ts`; `work_archive_client_header_guard_total` records accepted/missing guard outcomes with bounded `mode`, `method`, and `result` labels; `work_archive_rate_limit_exceeded_total` records rejections with a bounded `limiter` label, including `auth_sensitive` for sensitive auth operations; `docs/security/API_CACHE_POLICY.md` and `npm run qa:api-cache-policy` block API cache-control drift; `docs/security/CSRF_POLICY.md` and `npm run qa:csrf-policy` block drift in production Fetch Metadata, Origin allowlist, and CORS preflight CSRF controls.                                                                                                                                                                                                                                                                                                                                                             | Run client header audit before enforce and attach live accepted/missing metric evidence for legitimate client coverage.                                                                                                                                    |
| Configuration                    | V13 Configuration                                                   | partial   | Production rejects default secrets, short secrets, reused production secret values, placeholder OAuth/public URL values, localhost CORS origins, non-HTTPS production public URLs, `WEB_BASE_URL` origins missing from `CORS_ORIGIN`, OAuth redirect callback path drift or query/fragment values, metrics enabled without an explicit internal-access review flag, invalid/non-PostgreSQL/localhost database URLs, invalid/localhost Redis URLs, memory rate limiting, missing trust proxy hops, overlarge global API rate limits, overlarge sensitive auth operation rate limits, overlarge request body limits, overlarge readiness dependency timeouts, overlarge Node HTTP server timeouts, unsafe timeout ordering, and enabled Swagger in `api-runtime-config.ts`; beta and commercial env preflight scripts fail duplicate secret values before deployment; compose keeps `WORK_ARCHIVE_CLIENT_HEADER_GUARD` default at `audit`; `npm run qa:compose-hardening` blocks drift in production application service read-only filesystems, tmpfs scratch paths, dropped capabilities, no-new-privileges, application and stateful resource limits, and internal-only stateful services.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Attach config preflight output to release artifacts; finish user/capability hardening rehearsal for stateful Postgres and Redis before changing official image privileges.                                                                                 |
| Logging and error handling       | V16 Security Logging and Error Handling                             | partial   | `SecurityAuditService` records auth failures/success, logout, session revocation, blocked origin/fetch metadata/client header events, request-target/body/media type rejection events, rate-limit events, user data export, and account deletion/deletion failure events with hashed IP/user-agent context and sanitized metadata values, sensitive metadata key dropping, and inline OAuth/token key-value redaction; `apps/api/src/app.module.ts` redacts auth/cookie/set-cookie headers and strips request URL query strings/fragments before HTTP request logging; `ApiExceptionFilter` adds request IDs to API error bodies and returns sanitized generic bodies for unhandled 500s; direct security middleware rejections, body parser failures, unsupported media type failures, and rate-limit responses return sanitized JSON errors with request IDs before controller handling; `docs/security/LOG_REDACTION_POLICY.md`, `docs/security/API_ERROR_POLICY.md`, `npm run qa:log-redaction-policy`, and `npm run qa:api-error-policy` block drift in request URL sanitization, critical header redaction, security audit metadata sanitization, error response sanitization, body parser/media type/security middleware error sanitization, and regression tests; sync and import providers emit structured operational logs; sync push/pull outcomes and duration histograms use bounded labels; user data rights operations record `work_archive_user_data_rights_total` with only bounded operation/result labels; Gate 1 Prometheus alert rules are versioned in `docs/operations/monitoring/work-archive-alerts.yml` including user data rights failures, rate-limit rejections, client header guard misses, and sync latency; Gate 1 SLO rules are versioned in `docs/operations/monitoring/work-archive-slo-rules.yml`; the Gate 1 Grafana dashboard is versioned in `docs/operations/monitoring/work-archive-grafana-dashboard.json`; `scripts/qa/monitoring-evidence.mjs` collects redacted monitoring deployment evidence; `scripts/qa/validate-gate1-evidence.mjs` blocks public beta approval when live evidence placeholders remain.                                                                                                                                                                                                                                          | Deploy and route the alert/SLO rules, import the dashboard in the beta monitoring stack, run live monitoring evidence collection, prove retention cleanup in release notes, and keep logs free of tokens, OAuth codes, provider keys, and full image URLs. |
| Data protection and cryptography | V11 Cryptography, V14 Data Protection                               | partial   | JWT secrets and security event hash secrets are required in production; external provider credentials are encrypted via `ExternalApiKeyCryptoService`; refresh tokens are hashed; `GET /api/auth/data-export` returns user-scoped backend account data while omitting refresh token hashes, external provider encrypted keys, security event hashes, metadata, OAuth provider account ids, sync result JSON, Notion preview change payloads, and catalog submission payloads/notes; `GET /api/auth/account/deletion-preview` returns count-only deletion impact without row payload contents; `DELETE /api/auth/account` deletes the authenticated server-side account after explicit confirmation, records `auth.account.delete`, records rejected confirmations as `auth.account.delete_failed`, cascades account-owned rows, detaches retained operational references, and records bounded user data rights metrics without user identifiers; retention cleanup exists for `security_events`, `user_refresh_sessions`, `user_sync_applied_mutations`, and `notion_pull_preview_snapshots`; `docs/security/DATA_RETENTION_AND_PRIVACY.md` and `docs/security/USER_DATA_RIGHTS_POLICY.md` define historical client metadata, backup sensitivity, user data export, and Account deletion policy; `UserWorkRecord.userId` is required in the Prisma schema; `npm run qa:migrations` blocks unregistered high-risk Prisma migration SQL through `docs/operations/MIGRATION_RISK_REGISTER.md`; `npm run qa:owner-invariants` blocks owner-schema/documentation drift; `npm run qa:retention-policy`, `npm run qa:user-data-rights-policy`, and `npm run qa:user-data-rights-smoke` block privacy policy and smoke drift. | Add hosted release artifact proving retention dry-run/delete counts, backup storage controls, and account-deletion restore-drill evidence.                                                                                                                 |
| External service use             | V12 Secure Communication, V13.2 Backend Communication Configuration | partial   | Google OAuth endpoints and most import providers use HTTPS and bounded fetch timeouts; provider credential access is user-scoped; KOBIS HTTP provider is disabled unless explicitly enabled; `fetchExternal` and image proxy use HTTPS-only default-port URLs, provider allowlists, DNS resolution with private/special-use address rejection, timeouts, max bytes, content-type checks, and cache headers; `npm run qa:image-proxy-policy` blocks image proxy SSRF, redirect, non-default port, content-type, byte-limit, cache, and full-URL logging drift.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Add staging report-only telemetry before narrowing CSP `img-src`; keep provider allowlist changes reviewed as code changes.                                                                                                                                |
| Security scan evidence           | V13 Configuration, V15 Secure Coding and Architecture               | partial   | Root `package.json` keeps `security:audit`, `security:audit:prod`, `security:audit:prod:high`, `security:scan:fs`, `security:scan:images`, and `security:scan:release`; `docs/security/SECURE_SDLC.md` defines the high or critical production dependency release gate, vulnerability triage SLA, and waiver contract; `multer` is pinned to patched `2.2.0` through a scoped npm override, the dev/test `undici` path is updated to `7.28.0`, `js-yaml` is pinned to `5.1.0`, and local npm audit currently reports 0 vulnerabilities; scan recording template exists in `docs/security/SECURITY_SCAN_RESULTS.md`; `npm run qa:secure-sdlc-policy` blocks drift across the policy, release checklist, scan ledger, lockfile patch versions, and Gate 1 evidence ledger.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Run npm audit and Trivy on the release runner for every beta/production candidate and commit or attach summarized results.                                                                                                                    |

## Release Gate

Before public beta or production:

1. Update this matrix if a control status changes.
2. Update `docs/security/BOLA_MATRIX.md` for ownership-sensitive routes.
3. Attach scan summaries in `docs/security/SECURITY_SCAN_RESULTS.md`.
4. Re-run the validation commands listed in the release checklist and this
   task's completion report.
