# FRONTEND_FOUNDATION_MASTERPLAN.md

## 문서 목적
이 문서는 Work Archive를 단순한 기능형 프로젝트 화면이 아니라, **실용적이고 전문적인 실제 웹사이트**로 발전시키기 위한 프론트엔드 기반 구조 마스터플랜이다.

기존 문서들이 디자인 방향, 구현 계획, 현재 상태, 설계도 v1를 설명한다면, 이 문서는 다음 질문에 답한다.

- 왜 아직도 전체 사이트가 조잡하게 느껴지는가?
- 무엇을 먼저 바꿔야 실제 서비스 같은 느낌이 나는가?
- 기능을 더 넣기 전에 어떤 기반 구조를 먼저 세워야 하는가?
- 앞으로 확장성과 유지보수성을 확보하려면 무엇을 고정해야 하는가?

이 문서는 특히 **페이지 경험 분리**, **레이아웃 구조 재설계**, **도메인 책임 분리**, **전문적인 웹사이트 형태로의 전환**을 목표로 한다.

---

## 1. 현재 상태 요약

Work Archive는 이미 다음 수준까지 올라와 있다.

- 홈이 시작점 역할을 하기 시작했다.
- 작품 목록은 리스트 중심 관리 UX로 정리되고 있다.
- 작품 추가는 `검색 → 선택 → 자동 채움 검토 → 개인 기록 입력 → 저장` 흐름을 갖추기 시작했다.
- 휴지통/복원, 상세/리뷰, 게스트/계정, 동기화 흐름도 기초 구현이 존재한다.
- 설계도 v1도 고정되어 있다.

즉, 기능만 보면 단순한 초기 프로토타입 수준은 이미 넘어섰다.

그러나 현재의 웹은 여전히 다음 인상을 남길 위험이 있다.

- 프로젝트 느낌이 강하다.
- 화면이 분리된 서비스라기보다 하나의 큰 앱 안에서 내용만 바뀌는 것처럼 느껴진다.
- 로그인/회원가입도 별도 페이지라기보다 같은 구조 아래의 하위 블록처럼 보일 수 있다.
- 기능은 있지만, 실제 상용 웹사이트 같은 “페이지 경험”은 약하다.

---

## 2. 핵심 진단

### 문제의 본질
현재 문제가 단순히 "디자인이 덜 예쁘다"는 수준은 아니다.

핵심 문제는 다음과 같다.

1. **레이아웃 체계가 페이지 경험을 분리하지 못하고 있다.**
2. **기능별 책임이 화면 단위로 충분히 나뉘지 않았다.**
3. **라우트는 있어도, 유저는 여전히 같은 큰 셸 안에서만 움직이는 것처럼 느낀다.**
4. **실제 웹사이트보다 프로젝트 데모 앱 같은 인상이 남는다.**

즉, 지금 필요한 것은 “더 화려한 UI”가 아니라,
**실제 웹서비스처럼 페이지 맥락과 레이아웃 맥락을 분리하는 기반 공사**다.

---

## 3. 가장 먼저 해결해야 할 것

### 최우선 과제: 레이아웃 체계 분리
기능 고도화보다 먼저, 사이트 전체를 구성하는 **레이아웃 아키텍처**를 다시 세워야 한다.

현재는 라우트가 나뉘어 있더라도, 대부분의 화면이 같은 메인 셸 아래 들어가면서 다음 문제가 생긴다.
- 로그인도 메인 앱의 일부처럼 보임
- 작품 추가도 독립 플로우가 아니라 하위 화면처럼 보임
- 프로필/설정/동기화도 별도 목적 화면으로 덜 느껴짐

이 문제를 해결하려면 최소한 아래 4개의 레이아웃 맥락이 필요하다.

#### A. Main Product Layout
대상 화면:
- 홈
- 작품
- 티어 보드
- 인사이트
- 커뮤니티
- 프로필(공개형/사용자 중심 영역)

역할:
- 서비스의 핵심 제품 경험을 담당
- 메인 내비게이션을 포함
- 사용자가 “이 서비스 안에서 이동 중”이라고 느끼는 주력 셸

#### B. Auth Layout
대상 화면:
- 로그인
- 회원가입

역할:
- 메인 제품 셸과 분리된 인증 전용 맥락
- 사용자가 명확히 "인증 단계"에 들어왔음을 느끼게 해야 함
- 같은 페이지 안에 아래에 붙은 폼처럼 보이면 안 됨

#### C. Account Layout
대상 화면:
- 프로필 내부 관리 영역
- 동기화
- 설정
- 테마
- 공개 범위

역할:
- 계정, 환경설정, 동기화, 개인 설정을 담당
- 메인 콘텐츠 소비 화면과 분리된 계정 관리 영역

#### D. Minimal / Utility Layout
대상 화면:
- 404
- 특수 유틸리티 화면
- 추후 온보딩/특수 공개 페이지

역할:
- 최소 레이아웃으로 독립된 맥락 제공
- 메인 앱 셸을 불필요하게 가져오지 않음

---

## 4. 전문적인 웹사이트가 되기 위한 화면 책임 분리

레이아웃을 나누는 것만으로는 부족하다.
각 도메인도 내부적으로 **페이지 책임**이 분리되어야 한다.

### 4-1. 홈(Home)
역할:
- 서비스 시작점
- 기능 허브
- 개인 아카이브 진입점

하지 말아야 할 것:
- 설명이 많은 랜딩 페이지처럼 만들기
- 커뮤니티 정보를 과도하게 섞기
- 통계, 최근 기록, 빠른 추가, 행동 유도가 한 화면에서 뒤엉키게 하기

해야 할 것:
- 검색/빠른 추가
- 환영 메시지
- 핵심 통계 4개
- 최근 기록 6개
- 다음 행동 유도

즉, Home은 소개 페이지가 아니라 **행동 시작점**이어야 한다.

---

### 4-2. 작품(Works) 도메인
이 영역은 가장 중요한 작업 공간이다.

현재의 `works` 하나에 많은 역할이 묶이면, 다시 조잡한 구조로 돌아갈 수 있다.
따라서 작품 도메인은 내부적으로 다음 책임을 분리해야 한다.

- 작품 홈(선택적)
- 작품 목록
- 작품 그리드
- 작품 상세
- 작품 수정
- 작품 추가
- 휴지통

핵심 원칙:
- 작품 목록은 **작업 공간(workspace)** 이어야 한다.
- 작품 추가는 **플로우(flow)** 이어야 한다.
- 작품 상세는 **감상 기록 중심 detail page**여야 한다.
- 휴지통은 **수명주기 관리 페이지**여야 한다.

즉, 작품 기능은 한 화면에 몰리는 것이 아니라,
**관리 / 상세 / 추가 / 정리**로 경험이 분리되어야 한다.

---

### 4-3. 작품 추가(Add Work)
이 서비스의 차별점은 결국 **기록이 정말 편하다**는 인상을 주는 것이다.

따라서 작품 추가는 단순한 폼이 아니라,
서비스의 핵심 경쟁력을 담당하는 **독립 플로우 페이지**여야 한다.

최종 지향 흐름:
`검색 → 선택 → 자동 채움 검토 → 개인 기록 입력 → 저장`

이 페이지는 다음 요소를 갖춰야 한다.
- 검색 중심 진입
- 후보 비교
- 중복 경고
- 핵심 필드만 먼저 입력
- 저장 후 연속 추가 또는 방금 등록한 작품 보기

즉, Add Work는 “입력 페이지”가 아니라
**검색과 검증 중심의 기록 플로우**여야 한다.

---

### 4-4. 프로필(Profile)
프로필은 단순 유저 정보가 아니라, 장기적으로 **공개 취향 아카이브**가 되는 화면이다.

동시에 계정/설정/동기화와도 연결된다.

따라서 프로필에는 두 층위가 필요하다.

- 공개형 프로필(자기 표현, 리뷰, 공개 기록)
- 계정형 프로필(동기화, 설정, 테마, 공개 범위)

지금 단계에서는 적어도 이 두 성격이 섞이지 않도록 구조를 준비해야 한다.

---

### 4-5. 커뮤니티(Community)
커뮤니티는 장기적으로 중요하지만, 현재 중심은 아니다.

초기 방향은 다음이 맞다.
- 시끄러운 SNS형 피드가 아님
- 작품 단위 집계와 공개 정보 중심
- 인기 작품, 많이 기록된 작품, 인기 리뷰 중심의 정보형 허브

즉, 커뮤니티는 **개인 기록의 공개 레이어**에서 출발해야 한다.

---

### 4-6. 티어 보드(Tier Boards)
티어는 작품 필드가 아니라 **별도 기능**이다.

따라서 UI도 별도 경험으로 설계해야 한다.
- 보드 목록
- 보드 보기
- 보드 편집
- 공개 설정

일반 작품 관리 화면과 티어 편집 화면을 같은 패턴으로 섞으면 안 된다.

---

## 5. 앞으로의 우선순위

### 1단계: 레이아웃 아키텍처 분리
가장 먼저 해야 한다.

목표:
- 로그인은 로그인 페이지답게
- 작품 추가는 독립 플로우 페이지답게
- 프로필은 계정/공개 페이지답게
- 전체 사이트가 “한 페이지 안 내용 교체”처럼 느껴지지 않게 하기

---

### 2단계: 작품 도메인 페이지 책임 분리
목표:
- 작품 기능을 목록/상세/추가/휴지통 등으로 분리
- 작품 영역을 진짜 작업 공간으로 만들기

---

### 3단계: 작품 추가 실전화
목표:
- 외부 메타데이터 API 연동
- 후보 비교 강화
- 중복 방지 강화
- 자동 채움 신뢰도 향상

---

### 4단계: 공개 레이어 도입
목표:
- 작품 공개 집계 페이지
- 공개 프로필
- 공개 티어 보드

즉, 개인 기록이 자연스럽게 공개 가능한 결과물로 이어지게 만들기

---

### 5단계: 커뮤니티 확장
목표:
- 정보형 커뮤니티 허브
- 인기 리뷰 / 추천 흐름 / 가벼운 반응
- 필요 시 이후 댓글/팔로우로 확장

---

## 6. 전문적인 웹사이트를 만들기 위한 설계 원칙

### 원칙 1. 한 페이지에는 하나의 주 목적만 둔다
- 홈은 허브
- 작품 목록은 관리
- 작품 추가는 입력/검색 플로우
- 상세는 감상 기록
- 로그인은 인증

### 원칙 2. 같은 기능이라도 맥락이 다르면 레이아웃도 달라야 한다
- 메인 소비 화면
- 인증 화면
- 계정 관리 화면
- 유틸리티 화면

### 원칙 3. 설명보다 행동 유도가 먼저 보이게 한다
- 긴 설명 문구를 줄인다.
- 사용자가 지금 무엇을 할 수 있는지를 먼저 보여준다.

### 원칙 4. 예쁨보다 구조와 유지보수성을 우선한다
- 지금 구조가 확장에 약하면, 나중에 더 크게 망가진다.
- 먼저 구조를 세우고, 그 위에 시각 완성도를 올린다.

### 원칙 5. 현재 구조를 완성형으로 착각하지 않는다
- 현재 구현은 과도기적 단계다.
- 미래의 공개/커뮤니티/티어/가져오기 UX를 수용할 수 있게 설계해야 한다.

---

## 7. 최종 정리

Work Archive를 더 실용적이고 전문적인 웹사이트로 발전시키기 위해 가장 먼저 해야 할 것은,
더 많은 기능을 넣거나 더 화려한 UI를 만드는 것이 아니다.

가장 먼저 해야 할 것은 다음이다.

- 페이지 경험을 분리한다.
- 레이아웃 맥락을 분리한다.
- 도메인별 책임을 분리한다.
- 홈, 작품, 작품 추가, 프로필, 인증이 각각 실제 서비스다운 페이지 경험을 갖게 만든다.

즉, 지금 단계의 핵심 과제는:

**“프로젝트형 화면 구조를 벗어나, 실제 웹서비스다운 기반 구조를 세우는 것”**

이 문서는 이후 레이아웃 재설계, 라우트 그룹 재구성, 페이지 템플릿 설계, 도메인 책임 분리 작업의 기준 문서로 사용한다.
